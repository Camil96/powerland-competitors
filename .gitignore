# Automatische wekelijkse capture (Taakplanner) — lokaal, niet in git
# snapshots/ WEL in git: de evidence-laag (snapshot-van-die-dag links) moet live op Pages staan
capture-report.txt
powerland_capture.log
# Hermes plan-mode notities (lokaal, niet deel van dashboard)
.hermes/